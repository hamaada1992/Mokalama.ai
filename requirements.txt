streamlit==1.35.0
plotly==5.22.0

# نسخة متوافقة مع transformers الحديثة
transformers==4.41.2
tokenizers==0.19.1  # شرط من transformers

# نسخة متوافقة من faster-whisper لا تعتمد على tokenizers
faster-whisper==0.9.0  # لا يوجد شرط tokenizers <0.16

torch==2.5.0  # متاح لـ Python 3.13 (إصدار 2.3.0 غير موجود)
sentencepiece==0.2.0

# تحسين قراءة سجل الأخطاء
rich==14.0.0
